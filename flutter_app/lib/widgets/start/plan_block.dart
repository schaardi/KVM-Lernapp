import 'package:flutter/material.dart';

/// Prüfungstermin und Lernplan in der Heute-Karte (FR-006). Ohne Termin ein
/// schlanker Link „Prüfungstermin eintragen“, mit Termin der Plan.
/// Die Umsetzung folgt im Paket „Lernen“.
class PlanBlock extends StatelessWidget {
  const PlanBlock({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
